# КЛИК | Медиа-маркет — v6

Изменения:
- первичная регистрация только один раз;
- повторный /start сразу открывает кабинет по сохранённой роли;
- владелец всегда открывает админ-панель;
- реферальная ссылка не сбрасывает и не меняет уже созданный профиль;
- добавлена кнопка «Изменить профиль» для блогера и бизнеса;
- редактирование обновляет существующую запись, а не создаёт новую;
- PostgreSQL остаётся основной постоянной базой.

Railway Variables:
BOT_TOKEN
ADMIN_ID
DATABASE_URL
